# merge=union: beide Seiten werden vereinigt — Zeilen verschwinden nie still
# Suitable for append-only files (TODO, CHANGELOG). Duplicates are visible.
TODO.md      merge=union
CHANGELOG.md merge=union
